# 装饰器模式插件系统 - 架构说明

## 系统架构

### 核心设计理念

本系统采用**装饰器模式**（Decorator Pattern）结合**依赖注入**（Dependency Injection）和**配置驱动**（Configuration-Driven）的设计理念，实现了一个灵活、可扩展的插件系统。

## 架构分层

```
┌─────────────────────────────────────────────────┐
│              应用层 (Application)                │
│           Program.cs - 主程序入口                │
└─────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────┐
│          基础设施层 (Infrastructure)             │
│      PluginLoader - 插件加载和装饰器组装         │
└─────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────┐
│           配置层 (Configuration)                 │
│      PluginConfig - JSON配置模型                │
└─────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────┐
│         装饰器层 (Decorators)                    │
│   LoggingDecorator, CacheDecorator, etc.        │
└─────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────┐
│           插件层 (Plugins)                       │
│   TextProcessorComponent, DataValidator, etc.   │
└─────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────┐
│            核心层 (Core)                         │
│   IComponent, ComponentBase, DecoratorBase      │
└─────────────────────────────────────────────────┘
```

## 核心组件

### 1. Core（核心层）

#### IComponent 接口

```csharp
public interface IComponent
{
    string Execute(string input);
    string GetName();
}
```

**职责**：
- 定义组件的基本契约
- 所有组件和装饰器都必须实现此接口
- 保证了装饰器和被装饰对象的一致性

#### ComponentBase 抽象类

```csharp
public abstract class ComponentBase : IComponent
{
    protected readonly string ComponentName;
    public abstract string Execute(string input);
    public virtual string GetName() => ComponentName;
}
```

**职责**：
- 为具体组件提供基础实现
- 封装组件名称管理
- 简化组件开发

#### DecoratorBase 抽象类

```csharp
public abstract class DecoratorBase : IComponent
{
    protected IComponent _wrappedComponent;
    
    public virtual string Execute(string input)
    {
        string processedInput = PreProcess(input);
        string result = _wrappedComponent.Execute(processedInput);
        return PostProcess(result);
    }
    
    protected virtual string PreProcess(string input);
    protected virtual string PostProcess(string output);
}
```

**职责**：
- 实现装饰器模式的核心逻辑
- 提供前置处理（PreProcess）和后置处理（PostProcess）钩子
- 管理被包装组件的引用
- 支持装饰器链式组合

### 2. Configuration（配置层）

#### PluginConfig 模型

```csharp
public class PluginConfig
{
    public ComponentConfig BaseComponent { get; set; }
    public List<DecoratorConfig> Decorators { get; set; }
}
```

**职责**：
- 定义JSON配置的数据结构
- 支持组件和装饰器的配置
- 提供参数传递机制

### 3. Infrastructure（基础设施层）

#### PluginLoader 加载器

```csharp
public class PluginLoader
{
    public IComponent LoadComponent();
    private PluginConfig LoadConfiguration();
    private IComponent CreateComponent(ComponentConfig config);
    private IComponent ApplyDecorators(IComponent base, List<DecoratorConfig> configs);
}
```

**职责**：
- 读取和解析JSON配置文件
- 反射创建组件和装饰器实例
- 按优先级组装装饰器链
- 处理参数注入

**核心流程**：
1. 读取JSON配置
2. 创建基础组件
3. 按Priority排序装饰器
4. 逐个应用装饰器（从内到外）
5. 返回完整的装饰器链

## 装饰器模式实现

### 模式结构

```
┌────────────────────────────────────────┐
│         IComponent (接口)               │
│  + Execute(string): string             │
│  + GetName(): string                   │
└────────────────────────────────────────┘
          ↑                    ↑
          │                    │
          │                    │
┌─────────┴─────────┐  ┌──────┴────────────┐
│  ComponentBase    │  │  DecoratorBase    │
│  (抽象基类)       │  │  (抽象基类)       │
└───────────────────┘  └───────────────────┘
          ↑                    ↑
          │                    │
    ┌─────┴─────┐        ┌─────┴──────┐
    │具体组件   │        │具体装饰器   │
    └───────────┘        └────────────┘
```

### 装饰器链组装

```
基础组件: TextProcessor
装饰器: [Trim(10), Cache(20), Logging(30)]

组装过程:
1. component = new TextProcessor()
2. component = new TrimDecorator(component)       // 优先级10
3. component = new CacheDecorator(component)      // 优先级20
4. component = new LoggingDecorator(component)    // 优先级30

最终结构:
LoggingDecorator
  ↓ 包装
CacheDecorator
  ↓ 包装
TrimDecorator
  ↓ 包装
TextProcessor (核心组件)
```

### 执行流程

```
用户调用: component.Execute("  hello  ")
  ↓
LoggingDecorator.Execute()
  ├─ PreProcess: 记录输入
  ├─ 调用 → CacheDecorator.Execute()
  │         ├─ 检查缓存
  │         ├─ 缓存未命中
  │         ├─ 调用 → TrimDecorator.Execute()
  │         │         ├─ PreProcess: 去除空格 "  hello  " → "hello"
  │         │         ├─ 调用 → TextProcessor.Execute("hello")
  │         │         │         └─ 返回 "hello"
  │         │         └─ PostProcess: 返回 "hello"
  │         ├─ 存入缓存
  │         └─ 返回 "hello"
  └─ PostProcess: 记录输出，返回 "hello"
```

## 依赖注入机制

### 参数注入流程

```csharp
// 配置文件
{
  "TypeName": "MyDecorator",
  "Parameters": {
    "timeout": 5000,
    "retry": 3
  }
}

// 实际类定义
public class MyDecorator : DecoratorBase
{
    public MyDecorator(IComponent component, int timeout, int retry)
    {
        // component 由 PluginLoader 自动注入
        // timeout 和 retry 从 Parameters 读取
    }
}
```

### 注入规则

1. **装饰器**：第一个参数（IComponent）自动注入，其余参数从Parameters读取
2. **组件**：所有参数从Parameters读取
3. **类型转换**：自动处理基本类型的转换
4. **默认值**：支持构造函数的默认参数值

## 配置驱动架构

### JSON配置到对象的映射

```
plugins.json (JSON文件)
  ↓ (反序列化)
PluginConfig (C#对象)
  ↓ (反射加载)
Type (类型对象)
  ↓ (实例化)
IComponent (实例对象)
```

### 配置解析流程

```csharp
1. File.ReadAllText("plugins.json")
   ↓
2. JsonConvert.DeserializeObject<PluginConfig>(json)
   ↓
3. Type.GetType(config.TypeName)
   ↓
4. Activator.CreateInstance(type, parameters)
   ↓
5. 返回 IComponent 实例
```

## 扩展机制

### 1. 添加新组件

```csharp
// 步骤1: 创建类
public class MyComponent : ComponentBase
{
    public MyComponent(string name) : base(name) { }
    public override string Execute(string input) => input;
}

// 步骤2: 配置文件
{
  "BaseComponent": {
    "TypeName": "Namespace.MyComponent, Assembly"
  }
}
```

### 2. 添加新装饰器

```csharp
// 步骤1: 创建类
public class MyDecorator : DecoratorBase
{
    public MyDecorator(IComponent component, string name) 
        : base(component, name) { }
    
    protected override string PostProcess(string output)
    {
        return output.ToUpper();
    }
}

// 步骤2: 配置文件
{
  "Decorators": [{
    "TypeName": "Namespace.MyDecorator, Assembly",
    "Priority": 10
  }]
}
```

## 设计优势

### 1. 开闭原则（OCP）

- 对扩展开放：轻松添加新的组件和装饰器
- 对修改封闭：无需修改现有代码

### 2. 单一职责原则（SRP）

- 每个装饰器只负责一个功能
- 组件和装饰器职责明确分离

### 3. 依赖倒置原则（DIP）

- 所有组件依赖IComponent接口
- 降低耦合度，提高可测试性

### 4. 灵活性

- 配置驱动，无需重新编译
- 装饰器可动态组合
- 支持运行时调整

### 5. 可维护性

- 清晰的分层结构
- 职责明确
- 易于理解和维护

## 性能考虑

### 1. 反射开销

- 使用反射创建实例会有一定开销
- 可考虑使用对象池或工厂缓存优化

### 2. 装饰器链深度

- 过多的装饰器会增加调用栈深度
- 建议控制在5-10个装饰器以内

### 3. 缓存策略

- CacheDecorator 提供了简单的缓存机制
- 可根据需求替换为更高效的缓存实现

## 应用场景

### 1. 日志系统

通过装饰器添加不同级别的日志记录。

### 2. API网关

为API请求添加认证、限流、缓存等功能。

### 3. 数据处理管道

对数据进行清洗、验证、转换等一系列处理。

### 4. 中间件系统

类似ASP.NET Core的中间件管道。

### 5. 插件系统

支持第三方插件的动态加载和组合。

## 未来扩展方向

### 1. 异步支持

```csharp
public interface IAsyncComponent
{
    Task<string> ExecuteAsync(string input);
}
```

### 2. 泛型支持

```csharp
public interface IComponent<TInput, TOutput>
{
    TOutput Execute(TInput input);
}
```

### 3. 条件装饰器

```json
{
  "TypeName": "...",
  "Condition": "Environment == 'Production'"
}
```

### 4. 装饰器组

```json
{
  "DecoratorGroups": {
    "Logging": [...],
    "Security": [...],
    "Performance": [...]
  }
}
```

### 5. 依赖注入容器集成

与 Microsoft.Extensions.DependencyInjection 集成，支持更复杂的依赖管理。

## 总结

本系统通过装饰器模式、依赖注入和配置驱动三大核心设计理念，实现了一个灵活、可扩展、易维护的插件系统。系统遵循SOLID原则，具有良好的架构设计和代码质量。
